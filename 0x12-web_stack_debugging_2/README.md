<h1>0x12. Web stack debugging #2</h1>
<ul>
<li>Write a bash script that runs the whoami command under the user passed as an argument.</li>
<li>Write a Bash script that configures nginx to run as nginx user and listen on all active IPs on port 8080.</li>
<li>Using what you did for task #1, make your fix short and sweet.</li>
</ul>
